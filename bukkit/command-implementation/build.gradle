group 'dev.framework.bukkit.implementation.command'

dependencies {
  compileOnly project(':bukkit:framework-bukkit-bootstrap-implementation')
  compileOnly project(':framework-bootstrap')
  compileOnly project(':framework-commands')
}